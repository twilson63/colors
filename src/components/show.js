import React from 'react'
import { Link } from 'react-router-dom'
import { contains, reject, equals } from 'ramda'

export default ({ state, setState, match, history }) => (
  <div>TODO: Show Color</div>
)
